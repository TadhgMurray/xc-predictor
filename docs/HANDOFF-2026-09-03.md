# Handoff — 2026-09-03

Everything below is on `master` (last commit of this note's day: see §6).
`docs/ISSUES-2026-08-24.md` carries the long form (#130 onward are the last
two days). Read §0 first.

---

## 0. NEXT UP, IN ORDER

**State (2026-09-03, night): the rerun (`logs/20260903_035710`, pinned
curve, G = 0) finished in 3h 36m with 10_rankings, 13c_search_index and
17_checklist FAILED.** Steps 08 and 09b completed, so the race ratings,
athlete_ratings and course_difficulties on the site are this run's;
whether the boards are depends on where step 10 died (issue 144). The
owner's reading of the site afterwards, and what each turned out to be:

| owner saw | what it is | issue |
|---|---|---|
| track underrated against XC on dual-sport pages | the level cannot reach a race rating (it cancels); the curve's window pin was over every row of the pool, not the dual-sport rows that identify the level. Pin moved; needs the rerun | 147 |
| chair athletes back on the boards | fill_ratings priced every row the pack refused by person, and the boards only test the division labels. Fixed in code; this run's numbers stay until reset | 145 |
| Cam Kuss still there | 15/93: two people under one id, the twin rows on the other id. Nothing shipped for the merge; `twin_flag.py` (no --write) and `diag_merged_ids.py` still unread after a run | 15, 93, 137 |
| difficulty column contradicts the rating | a corrected division shows a cell it never used (page fixed); the race-day effect is in the rating and not in the column (by design, needs a place on the page); `scripts/explain_joint_row.py` says which per result | 146 |

1. **Read the three failed logs before anything else** (nobody has):

   ```
   tail -40 logs/20260903_035710/10_rankings.log      # before or after "swapping in..."?
   tail -30 logs/20260903_035710/13c_search_index.log
   grep -n 'FAIL\|WARN' logs/20260903_035710/17_checklist.log
   ```

   If 10 died before the swap, /rankings is last week's build and nothing
   on a board says anything about this run. If after (buildBoardSizes,
   the VACUUM), the boards are this run's and only board_size is missing.
   13c is not the sequence name (checked on a local Postgres 16: the
   second CREATE just picks `search_index_new_id_seq1`). Fix what the
   logs say, then:

2. **Rerun from 6: the pack must carry `dist_m` for the track distance
   offset (148), and the pin moved (147). The owner states a 2% winter
   gain (measured 1.1-1.3% for hs_m; the owner: "I'm certain ppl get abt
   2% better").**

   ```
   cd /srv/xc-predictor && git pull -q origin master
   tmux new -s pipe
   XCP_WINTER_GAIN=0.02 XCP_JOINT_LIVE=1 bash deploy/run_pipeline.sh --from 6 2>&1 | tee logs/run8.out
   ```

   2026-09-04, late: geometry correction OFF by default (176) -- the
   from-0 run re-backfills without it; event offsets by rating band
   (167) on by default; the altitude term (172) still behind --altitude.

   2026-09-06: the winter gain is stated PER RATING BAND and applied
   to the track rows at go-live (194, `XCP_WINTER_GAIN_BANDS`, the
   owner's number; 0.03,0.02,0.03 proposed), on top of the pairs
   calibration of the event offsets (190) and the race-day cap (187).
   Next run:

   ```
   XCP_WINTER_GAIN=0.02 XCP_WINTER_GAIN_BANDS=0.03,0.02,0.03 XCP_ALTITUDE=1 XCP_JOINT_LIVE=1 bash deploy/run_pipeline.sh --from 8 2>&1 | tee logs/run12.out
   ```

   Read in 08_golive's log: the `winter gain per band` table (gap read,
   target, shift per pool and band), the `pairs` rows under the offsets
   table, the `race-day cap` line. Then `diag_cross_sport_pairs.py
   --since 2024` (TF-XC per bucket should read about +the stated gain in
   points, minus a point for the XC best being the max of more races),
   and the 9:01 / 4:13 / Mt. SAC 15:40 on the site. The region question
   is answered (191: no offset worth a prior; altitude as a visitor
   effect parked, 192; TF state codes dirty, 193).

   2026-09-04, night: gender by the rows and the split (164) is in code
   and needs step 04d + the pack: `--from 0` for the backfill to see it,
   `--from 6` for the engine and the boards. The cross-date twin rule
   (158) is in 04c. Everything on the athlete page (team links, season
   school, HS-equivalent) only needs a site restart.

   2026-09-04, later: run9 was TWO pipelines at once (157: flock + advisory
   lock now). Before launching: `tmux ls`, kill every stale pipeline
   session, `ps aux | grep '[r]un_pipeline'` must show nothing. The fill
   and the search index are cut down; the rowguard diags run in parallel.

   2026-09-04: run9 (slope + link) deflated the top by 7% -- the link
   (155, now off by default) -- and diag_cell_days exposed the race-day
   degeneracy (156, u now tilted like the course). Its go-live FAILED
   after 5.4 h mid-write (traceback unread: `tail -30 $(ls -td logs/*/ |
   head -1)08_golive*.log`) and 09b/10/10c/12b failed in cascade. Next
   run: --from 8, same command, link off, u tilted.

   The night run (winter gain 0.02, the offsets) is DONE; its numbers
   are in 148 and 154. The next run carries the endurance slope and
   the season link (154) and needs only --from 8:

   ```
   XCP_WINTER_GAIN=0.02 XCP_JOINT_LIVE=1 bash deploy/run_pipeline.sh --from 8 2>&1 | tee logs/run9.out
   ```

   Read: the blocks line (`endurance slope ON, season link ON`), the
   `[joint] endurance slope` line (share exactly 0 = single-distance
   seasons; |g| mean a few hundredths), `vs pair_difficulty`, and the
   same diag_sport_gap tables. Chair athletes: step 04b was skipped by
   --from 6, so wheelchair_person may be empty; build it first
   (`engine/wheelchair_flag.py --write`) or the boards keep them.

   Steps 10 and 13c failed on the 2026-09-03 run for two code faults,
   both fixed (151, 152). Step 17 will still say FAIL: that is issue
   132, the owner's call, not a new failure (153).

   Step 07 repacks (the cache was cleared by 06). Read in `08_*.log`:
   `track distance offsets ON (N classes)` on the blocks line, then the
   `[joint] track distance offsets` table -- expect hs_m 800 negative and
   3200 positive by a few hundredths, the 1600 pinned; `level mu` (still
   about -0.05) and `curve TF-XC window gap` (-0.02 per pool: the stated
   gain, now measured over dual-sport rows). After the run
   `diag_sport_gap.py --pool hs_m --tf-event '3200|2 mile'` and the 800
   should agree within a point at every band; the slope that remains is
   decision 2 of issue 148.
   The go-live resets speed_rating, so the chair rows this run priced go
   with it; 09b now prints `wheelchair_person: N chair athletes excluded
   by person` (a bare-table warning instead means 04b never ran on this
   database). After the run:

   ```
   /srv/venv/bin/python scripts/diag_sport_gap.py      # expect a mean near 0 with G = 0
   /srv/venv/bin/python scripts/explain_joint_row.py --xc <ids from the owner's tables>
   ```

   The explain script is the answer to "these ratings don't make sense
   for these difficulties": per result it prints in-the-solve / no-cell /
   not-in-the-pack, the cell, h, the race-day u, and rating x adjusted
   time, which every solved row of one athlete shares.

   Quicker path for the chair rows alone, without a solve: the two
   UPDATEs in issue 145, then `--from 10`.

3. **Measure the winter gain, after the rerun** (`scripts/diag_winter_gain.py`,
   rows-weighted lines at the bottom; expect 0.01-0.025 for HS pools).
   The owner has NOT chosen a number; 3% was a round guess. With the pin
   over dual-sport rows, G is by construction the track-minus-XC gap a
   dual-sport page shows, so diag_sport_gap after a run with
   `XCP_WINTER_GAIN=G` reads about +100 x G.

4. **The run after that carries the number:**
   `XCP_WINTER_GAIN=<value> XCP_JOINT_LIVE=1 bash deploy/run_pipeline.sh --from 8`.

5. **Read `10c_gap` from the rerun** for the level check (#133); it lands
   after 10_rankings and 10b_school_ids.

6. **Confirm the HS-equivalent factor on the live site** (§1):
   `scripts/diag_hs_factor.py`; ms_m about 0.8, home page MS boards near 140.

7. **Revoke the GitHub token** in the server's remote URL (pasted three
   times). Revoke on GitHub, new token, `git remote set-url origin ...`.

8. After the rerun: `engine/twin_flag.py` (no `--write`) and
   `scripts/diag_merged_ids.py`, read the counts (#137, #138); Cam Kuss
   (15/93) is unfixed until the merged id is split.

**Code since the morning note (all on master, tests pass):**
`wheelchair_flag.ensureTable`; `build_ranking_results.ensureWheelchairPerson`
and the person anti-join in both `_SQL` halves (the pricer inherits it);
`run_checklist` reads wheelchair_person; `_chairFilter` treats an empty
table as absent; `joint_solve.curveGapVectors` pins over dual-sport rows;
the athlete page blanks the difficulty of a corrected division;
`scripts/explain_joint_row.py`; `tests/test_chair_person_everywhere.py`,
`tests/test_curve_gap_dual_rows.py`. Later the same night: the track
distance offset in the joint solve and the pack's `dist_m` (148,
`tests/test_joint_dist.py`), `diag_sport_gap` bands and filters, the
favicon on three pages (149), issue 150 opened.

---

## 1. THE HS-EQUIVALENT FACTOR (#139), THIRD CUT, AND WHAT IT GUARANTEES

**The bug.** The owner saw a middle-school board of both sports, sorted on
HS-equivalents, put a 142.2 XC season under a 138.2 track season, and the
same across gender on team rankings. The factor was per (pool, sport,
distance), so rows of one pool wore different multipliers and the HS view
reordered them.

**Three cuts in two days:**

| cut | factor | outcome |
|---|---|---|
| 1 (09-02) | C(hs)/C(pool), C sampled over both sports | MS boards read 220: constants are in per-pool units |
| 2 (09-03) | geomean over sports of bare C(hs)/C(pool) | ms_m 1.41, still wrong, same cause |
| 3 (09-03, live) | geomean over sports of C(hs)/C(pool) x F(REP_DIST[sp],pool)/F(REP_DIST[sp],hs) | ms_m about 0.8 |

The F term is a unit conversion: each pool normalises to its own reference
distance (ms about 3000 m, hs 5000, college_m 8000, college_f 6000), so the
pool constants are not on one scale and the bare ratio is meaningless. The
old per-sport factor evaluated at the sport's reference distance (5000 XC,
1600 TF) is the right per-sport number; the geometric mean of the two is
the one number per pool. `_poolConstant` is per sport only and its
docstring says never to merge sports. `scripts/diag_hs_factor.py` prints
every piece.

**Does one number per pool remove the sport offset and the gender case? Yes,
by construction:**

- **Sport.** `hsFactor(pool, sport, distance)` ignores sport and distance
  and returns the pool's one number. Inside a pool the HS view is
  `own x k`, a monotone rescale, so no board that mixes XC and TF rows of
  one pool can reorder. The rankings ORDER BY (`rankings._hsScaleCase`)
  still has a CASE arm per (pool, sport), but both arms of a pool carry the
  same value now. `stampBoardRows` takes the distance branch or the
  representative branch; both land on the same number.
- **Gender.** Pools are gendered (ms_m, ms_f, college_m, college_f), one
  factor each. A team board is one pool by construction (`teams.py`:
  no `both`, no `all`; "high school boys and college women" are refused in
  one field), so on a team board the factor is a constant and a finish
  order scored on places is invariant to it. Across genders the two
  factors legitimately differ: ms_m maps onto hs_m, ms_f onto hs_f. That
  is the intended conversion, not an offset. The one list that mixes
  genders is a school roster, and `sortByShown` orders it on the numbers
  shown.
- **What is not fixed by it.** The factor is one number per pool, so the
  ratings it multiplies still carry whatever level error the engine has
  between sports (the 10c gap in §0). The factor cannot reorder rows; it
  also cannot correct them.

Caveats: `_FACTOR_CACHE` is per process, so the site needs a restart to
pick up new constants; the constants sidecar refreshes at step 13b. The
sanity rail is 0.5-2.0; a factor outside it returns None and the row keeps
its own scale (never 1.0-with-a-shrug, see the docstring).

Tests: `tests/test_hs_factor_per_pool.py` (six: same factor every
sport/distance, hs pools 1.0, rail, two scales never merge, reference
distances converted, source pins on `_REP_DIST[sp]` and `np.log(ratios)`),
`tests/test_scale_sort.py`, `tests/test_predict_squad_cap.py`.

---

## 1b. THE FIRST LIVE JOINT RUN AND ISSUE 143

**What the run said** (`logs/20260903_014116/08`): the level climbed every
outer, 0.021, 0.048, 0.064, 0.074, 0.079, 0.081, and finished at
mu[TF] = +0.081 against the sequential engine's applied gap of -0.047.
Cells moved with it (vs pair_difficulty corr 0.36, median |move| 0.065).
`scripts/diag_sport_gap.py` over 189,657 people rated in both fall-2024
XC and spring-2025 track: mean TF - XC rating +14.56, median +14.0. So
the fit had decided runners gain 13% over the winter and the track
surface is 8% harder than XC.

**Why:** the between-sport level and the curve's fall-to-spring step are
collinear on every row except the December-to-January bridge. The
smoothness prior decided the split, decided it wrong, and the slow CG
direction meant six outers were still drifting toward an optimum further
out. The synthetic test passed because its world generates a curve that
is smooth through the winter and 40% of its athletes race indoor.

**Fix (`engine/joint_solve.py`, CURVE_GAP_WEIGHT = 100 x rows per pool;
`run_joint --curve-gap`):** a rank-one-per-pool penalty pinning the curve's
row-weighted mean of amp*f over track rows to its mean over XC rows. The
curve is within-window form only; mu carries the whole between-sport
difference. That is the sequential engine's identification (the average
dual-sport athlete has no sport preference) with the within-season shape
kept, so the late-season confound (Balboa, Woodward Park reading easy
because November fields are fit) stays fixed. The direction is stiff now
and the level settles in the first outer. `--curve-gap 0` is the old
split. The outer log prints `curve TF-XC window gap`.

**Owner: "so this is just gonna go back to our old issue?"** Yes for the
level (#113), no for the late-season courses. The middle way is the
stated winter gain: `--winter-gain G` / `XCP_WINTER_GAIN=G` pins the
track window G below the XC window and the level carries the rest, so
the average athlete's spring ratings sit G above their fall ones BY
ASSUMPTION, printed in the log. Default 0. `tests/test_joint_year.py`:
the gap pins to ~0, the level reads surface + the curve's net step, G
moves the level by exactly G; the pure-recovery test now passes
`curve_gap=0` explicitly.

**The number** comes from `scripts/diag_winter_gain.py` (§0 item 2):
same-sport annual gain minus the curve's two in-season moves, split
winter/summer three ways. Survivors only, grades mixed, curve at rating
100. A range, not a point. `tests/test_diag_winter_gain.py` covers its
pure pieces; the DB half is unexercised until the owner runs it.

---

## 2. THE JOINT SOLVER AND THE PIPELINE SINCE THE LAST HANDOFF

**Solver (`engine/joint_solve.py`, `engine/run_joint.py`), #133/#140:**

- Every outer iteration solves to `CG_TOL` (1e-8). The loose early outers
  (`CG_TOL_OUTER`) are gone: they let the level swing 0.03 on the last step.
- Posterior probes: `CG_TOL_PROBE` 1e-4, `CG_MAX_ITER_PROBE` 150, a printed
  line per probe with its iteration count and time, `--probes 0` returns
  the information-diagonal bound (`sigma2/diag`; exact on a diagonal
  operator, `tests/test_joint_speed.py`). The live step runs `--probes 4`.
  `cell_se` is reporting only; nothing on the site reads it.
- Rows are sorted by athlete before the design is built
  (`sortRowsByAthlete`), block reductions run on a thread pool
  (`_N_THREADS`), no held-out solve on the live step (08b shadow keeps
  `--holdout`).
- **Still structurally slow:** the level direction (one national XC/track
  parameter) is the slow mode of the CG; it is why probes hit the cap. A
  coarse-space correction (solve the level and the pool means exactly
  inside the preconditioner) is the fix. Parked, not started.
- The sequential engine has only the tilt; the year form curve exists only
  in the joint solve. There is no fitness curve on the sequential path.

**Pipeline (`deploy/run_pipeline.sh`):**

| step | what | note |
|---|---|---|
| 04c_twins | `engine/twin_flag.py --write` into `result_twin` | only on a full run; `--from 8` skips it |
| 08 | `run_joint.py --golive --probes 4` when `XCP_JOINT_LIVE=1` | no `--holdout` |
| 08b | shadow, keeps `--holdout` | `XCP_JOINT=1` |
| 10e_meet_units | `build_meet_units.py` | championship meets and their units |
| 11b_indexes | page indexes (`add_page_indexes`) | before 12, was step 14 |
| 12b | `build_course_boards --prepare`, then three `--shard K/3`, then `--finish` | `shards()` helper, one log each; 41,047 s before |
| 13c_search_index | `search_index.py` into `search_index_new`, swapped | search never dark |
| 13d_sitemap | `build_sitemap.py` | writes `static/sitemaps/` (gitignored) |

Rankings build (#57, #136): `board_size` is written after the swap and a
`VACUUM (ANALYZE)` of the swapped tables; rating-ordered indexes
(`rr_pool_rating_idx`, `rr_pool_sport_rating_idx`, `as_pool_mean_idx`,
`as_pool_sport_mean_idx`) let page one of a default board stop after fifty
rows. The pager's Last button reads `board_size` for default boards and
counts live under an 8 s `statement_timeout` for the rest, saying so when
it cannot.

---

## 3. SITE CHANGES SINCE THE LAST HANDOFF (all live on restart)

- **Season floor is a fact about the season** (`racecast/season_floor.py`,
  #131): three races up to 2026 TF, none from 2026 XC on (stored year
  `OPEN_FROM=2026`). The browser sends `min_races` only when touched. A
  silly one-race season on a live board is dropped by hand; agreed cost.
- **Non-finish letters** (#59): `results.status` column, DNF/DNS/DQ shown
  as letters; TF marks keep their letters.
- **600 m is rated** (#42).
- **Conversions follow the athlete's pool and the rating scale** (#49):
  `repFactor` scales hs to pool; `base_rating_hs` on the response.
- **Championship meets and units** (#34): `meet_unit` table, /meets filters
  champ + unit, units in site search.
- **Data identity** (#94/#15/#68/#93, #137/#138): `result_twin` reasons
  twin_race / twin_person / dup_same_feed; anti-joins in the engine
  (`_dedupJoin`), the boards (`_SQL` NOT EXISTS), `fill_ratings`, and the
  athlete page. `scripts/diag_merged_ids.py`; `scripts/link_idless_by_name.py`
  refuses a gender contradiction (Postgres word boundary is `\y`).
- **Athlete page on phones** (#88): 2x2 stat strip, rank entries never break
  inside, toggle label above the buttons; the 5K/percentile line moved to
  All-Time Bests; unit chips biggest first, no bare class chip.
- **Home board** rating explainer; school "Every race" view-all under the
  table.
- **Google / Cloudflare** (#141, `docs/SEO.md`, `docs/CLOUDFLARE.md`):
  `/robots.txt`, `/sitemap.xml`, canonical tag on `XCP_SITE_ORIGIN`,
  `/google3e7b9f167371bc58.html` served for the one listed token
  (`GOOGLE_VERIFY_TOKENS`), `deploy/cloudflare_realip.sh` so nginx logs the
  visitor's IP. Owner side still open: Search Console verify + sitemap
  submit, the www redirect rule, Bot Fight Mode (owner said yes).
- **Visits:** GoAccess over the nginx log. About 80 human visitors a day;
  ClaudeBot and GPTBot are the volume, allowed by the owner's choice.

---

## 4. PARKED, BY THE OWNER

- Alerting; rating uncertainty on the site; the screenshot harness as a
  test (the mock harness lives in the session scratchpad only).
- The cross-state XC-level measurer (write once the pipeline has run:
  difficulty by state per sport from the joint file, venue -> state via
  `course_canonical`).
- Coarse-space correction for the level direction (§2).
- Non-US scope (#40); #96 unknown; #27 no.
- #91 / #108 (build_ranking_results, team rankings): look after the run.

---

## 5. THINGS THAT BIT, SO NOBODY REPEATS THEM

- Edit scripts that assert on an anchor string and silently do nothing
  when it is absent shipped a test without its code once (505f7eb) and a
  vacuum block outside its connection block once (e0372bc). Check
  `git show --stat` after every scripted edit.
- The rankings page called a function that never existed
  (`syncGenderField`) and the first paint died. Gone.
- A 502 right after `systemctl restart` is the workers booting. Wait ten
  seconds.
- The GitHub PAT is in the server's remote URL and shows up in any pasted
  `git` error. See §0 item 4.

---

## 6. TODAY'S COMMITS, IN ORDER (09-02 evening to 09-03)

| | |
|---|---|
| `d52b325` | joint solve: no held-out on live, looser probe tolerances, sorted rows, thread pool |
| `ae91d78` | 59 letters, 49 scale-aware conversions, 600 m |
| `06ad917` | championship meets and units |
| `6dd71fc` | data identity: result_twin, merged-id diag, linker gender guard |
| `8a79e7e`, `d7694b4`, `e95a987` | Last button, board_size + vacuum, rating-ordered indexes |
| `505f7eb`, `bf324dd` | one HS factor per pool (cut 1), 12b shards, search index shadow |
| `ada0ed9` | every outer tight again |
| `11f018b`, `cd6cdc5`, `15640d4` | Cloudflare, robots/canonical/sitemap, Google file |
| `b6bed26`, `bb93351`, `917174f` | probe line, probe cap 150, `--probes 0` bound, live `--probes 4` |
| `0d31b01`, `e4ecb79` | HS factor cut 2, `diag_hs_factor.py` |
| `5d0d959` | HS factor cut 3: F at each sport's reference distance |
| `ee423f4` | this handoff, first cut |
| `20c1573`, `0c4f3be` | best race on the shown scale (142); issue 143 opened; diag_sport_gap |
| `3f21d8b` | joint solve: the curve carries no net level between the sports' windows (143) |
| `4ecdd6c` | the stated winter gain (--winter-gain, XCP_WINTER_GAIN) |
| `ac2b830` | diag_winter_gain |

## 7. ISSUE LIST

New since the last handoff: #130-#143. Open and live: **#143 (XC
underrated on the live site until the rerun; then the winter-gain
number)**, #133 (level, read 10c), #139 (confirm on the site), #140 (first
sharded 12b run untimed), #141 (owner side), #113 (the definition,
answered as far as the data allow: within-season form is honoured, the
winter gain is stated). Fixed today: #142. Long form in
`docs/ISSUES-2026-08-24.md`.
